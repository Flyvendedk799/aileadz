__pycache__/
instance/
static/temp/
static/uploads/
*.mp3
*.pptx
*.zip
*.db
*.db-shm
*.db-wal
*.json

# Confidential internal roadmap — enumerates security weaknesses; keep out of the public repo
docs/PLATFORM_ROADMAP.md

# stray run log (contains old creds; untracked, pending history purge)
run_history.txt

# AI eval run artifacts (regenerated; baseline established in-environment)
ai_eval/last_run.json
ai_eval/baseline.json
